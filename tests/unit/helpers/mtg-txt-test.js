
import { mtgTxt } from 'overseer/helpers/mtg-txt';
import { module, test } from 'qunit';

module('Unit | Helper | mtg txt');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mtgTxt([42]);
  assert.ok(result);
});

